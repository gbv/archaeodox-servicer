version: "3.8"

services:
  servicer:
    container_name: servicer
    build:
      context: .
    restart: unless-stopped
    ports:
    - 5001:5000
    networks:
      hostnet: {}
    volumes:
    - ./app:/opt/app
    - /srv/easydb/eas/lib/assets/orig/0:/eas_assets
    - /home/trapp/geoserver-dc/geoserver_data/data/shapefiles/field_imports:/field_imports

networks:
  hostnet:
    external: true
    name: easydb_default

